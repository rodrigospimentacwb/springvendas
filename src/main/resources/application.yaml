spring:
  profiles:
    active: development

---

spring:
  config:
    activate:
      on-profile: development

server:
  port: 8081

---

spring:
  config:
    activate:
      on-profile: production